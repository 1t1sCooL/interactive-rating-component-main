apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: interactive-rating-component-main
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /InteractiveRatingComponent
            pathType: Prefix
            backend:
              service:
                name: interactive-rating-component-main
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
